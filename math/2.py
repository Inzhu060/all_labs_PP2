import math

height = float(input("Enter the height: "))
base_first = float(input("Enter the base1: "))
base_second = float(input("Enter the base2: "))
area = (base_first + base_second) * height / 2
print(area)